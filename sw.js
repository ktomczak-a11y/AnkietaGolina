const CACHE='wizja-pv-v11';const ASSETS=['./','index.html','styles.css?v=10','beneficiaries.css?v=10','app.js?v=10','manifest.webmanifest','icon.svg','nasz-prad-logo.png','https://cdn.jsdelivr.net/npm/jsqr@1.4.0/dist/jsQR.js'];self.addEventListener('install',e=>{self.skipWaiting();e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)))});self.addEventListener('activate',e=>e.waitUntil(Promise.all([self.clients.claim(),caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))),self.clients.matchAll({type:'window'}).then(list=>Promise.all(list.map(client=>client.navigate(client.url))))])));self.addEventListener('fetch',e=>{if(e.request.mode==='navigate'){e.respondWith(fetch(e.request).then(r=>{let clone=r.clone();caches.open(CACHE).then(c=>c.put('./',clone));return r}).catch(()=>caches.match('./')));return}e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request)))});
